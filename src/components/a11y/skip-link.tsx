/**
 * Enlace para saltar al contenido principal (teclado / lectores de pantalla).
 */
export function SkipLink() {
  return (
    <a href="#contenido-principal" className="skip-link">
      Saltar al contenido
    </a>
  );
}
